using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGenerator : MonoBehaviour
{
    /// <summary>
    /// This variable holds a list of the game objects, the function GenerateBoundaries() will make a shape around them that forms a boundary
    /// </summary>
    [SerializeField] GameObject[] mapBoundaries;
    // https://dev.to/bogdanalexandru/generating-random-points-within-a-polygon-in-unity-nce

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
